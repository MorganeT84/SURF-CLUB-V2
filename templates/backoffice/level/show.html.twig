{% extends 'Backoffice/baseBackoffice.html.twig' %}

{% block title %}Niveau
{% endblock %}

{% block body %}

	<div class="main">
		<h1>Niveau</h1>
		<button type="button" class="btn btn-warning">
			<a href="{{ path('app_backoffice_session_index') }}">Retour Session</a>
		</button>
		<button type="button" class="btn btn-warning">
			<a href="{{ path('app_backoffice_level_index') }}">Retour à la liste</a>
		</button>

		<table class="table table-light table-bordered table-hover">
			<tbody>
				<tr>
					<th>Nom</th>
					<td>{{ level.name }}</td>
				</tr>

			</tbody>
		</table>
		<button type="button" class="btn btn-danger">
			<a href="{{ path('app_backoffice_level_edit', {'id': level.id}) }}">Modifier</a>
		</button>
		{{ include('Backoffice/level/_delete_form.html.twig') }}
	{% endblock %}
